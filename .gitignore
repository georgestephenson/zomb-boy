# Repo-local pinned toolchain (installed by `make tools`, not committed)
.tools/

# Build output
build/

# Python (test harness) cache
__pycache__/
*.pyc

# Editor / OS cruft
*.swp
.DS_Store

img/portrait/source/
img/portrait/backup/
img/zombie/source/
img/source/

# Generated tile atlas — derivative of src/gfx.asm; regenerate with
# `python3 tools/export-tiles.py`. Not committed (keeps the repo small).
img/tiles.png